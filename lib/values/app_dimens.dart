import 'package:flutter/cupertino.dart';

class AppDimens {
// border radii
  static const BorderRadius radiusBorderRoundedCorner =
      BorderRadius.all(Radius.circular(radiusRoundedCorner));

  // radii
  static const radiusRoundedCorner = 12.0;
  static const double sizeLoadingIndicator = 64.0;
  static const double sizeAuthIcon = 48.0;

  // indents
  static const dividerIndent = 4.0;
}
